# buildwitharyo

My portfolio site — fullstack dev & AI automation work. Built with Next.js (App Router),
Tailwind, and Framer Motion, deployed on Vercel.

## Run it

```bash
npm install
npm run dev
```

Then open http://localhost:3000.
